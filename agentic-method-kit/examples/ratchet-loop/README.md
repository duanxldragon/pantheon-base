# Ratchet Loop Example

This example shows a minimum method-evolution loop:

1. a repeated failure is observed
2. the failure is recorded in a failure registry entry
3. the harness changes through a guide, sensor, gate, or template update
4. the next task uses the updated harness

Use this example as a pattern for turning recurring agent mistakes into durable repository controls.
