# Runtime And Template Example

This example shows how a repository can:

- declare its template selection
- attach runtime-sensitive coverage to a task
- record runtime evidence or a documented runtime gap
- keep the closure between task packet, evidence, and review artifact intact

Use it as a starter for repositories that want lightweight runtime-quality governance without adopting a specific observability stack.
