# Runtime 与 Template 示例

这个示例展示仓库如何：

- 声明自己采用的 template
- 在任务中标记 runtime-sensitive 覆盖
- 记录 runtime evidence，或显式记录 runtime gap
- 保持 task packet、evidence 和 review artifact 的闭环

适合给那些想要轻量 runtime-quality 治理、但又不想绑定某个可观测性栈的仓库做起点。
