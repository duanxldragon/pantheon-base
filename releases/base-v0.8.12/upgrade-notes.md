# Upgrade Notes

Ops can upgrade to base-v0.8.12 without changing single-instance defaults. Enable PANTHEON_CASBIN_WATCHER=true and PANTHEON_TOKEN_CACHE_TTL_SECONDS=0 only when multi-instance consistency is required.
