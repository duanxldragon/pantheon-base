# Consumer Impact

pantheon-ops should consume this release instead of syncing directly from pantheon-base main; inherited auth, security, upload, and release tooling changes should come from the artifact.
