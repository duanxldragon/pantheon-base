# Upgrade Notes

Consumers can stay on the current release if no shared-module sync is needed. For future upgrades, use the tagged foundation release as the inheritance baseline and rerun existing drift checks.
