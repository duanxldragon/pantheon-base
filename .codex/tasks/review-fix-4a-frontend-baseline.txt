在 pantheon-base/frontend 为即将重构的大组件建立回归基线，本批不做拆分，只补测试与基线。

目标组件：
- src/core/layout/index.tsx（约 2103 行）
- src/modules/system/i18n/I18nList.tsx
- src/modules/system/dept/DeptList.tsx
- src/modules/lowcode/generator 下的 ModuleWizard
- src/modules/system/role/RoleList.tsx

步骤：
1. 跑 npm run typecheck 与现有 smoke（tests/smoke），记录当前通过基线。
2. 为上述组件中缺 smoke 覆盖的核心交互补 Playwright smoke：渲染、主菜单展开、tab 切换、权限受控按钮显隐。
3. 不改动任何组件实现。

完成后运行 npm run lint && npm run typecheck 并贴结果。输出：新增测试清单 + 基线通过情况。
