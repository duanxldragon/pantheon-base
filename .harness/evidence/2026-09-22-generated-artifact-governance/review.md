# Review — 2026-09-22-generated-artifact-governance

## Reviewer disposition

Reviewed as a platform/governance + generated-artifact change (generated-governance
profile, low risk). Verdict: **approved**.

## Checks

| Question | Answer |
| --- | --- |
| Do generated files carry a stable marker? | Yes — the exact first line `// Code generated by pantheon low-code generator. DO NOT EDIT.` is on all 11 text artifacts; JSON is identified by path + parseability. |
| Does CI detect generated drift? | Yes — `check:generated` is a blocking quality.yml step; marker removal, wrong line position, missing artifact, or invalid JSON all fail. |
| Is manual vs generated ownership documented? | Yes — `REPOSITORY_LAYOUT.md` §7.1 lists source, outputs, and the `business/*` hand layer; §7.2 defines the marker and gate. |
| Does the marker survive regeneration? | Yes — `GENERATED_MARKER`/`markTemplate()` stamp the templates, and `cleanup` restores tracked registries from the git index (which will carry the marker once committed). |
| Does the gate false-positive on legitimate generated modules? | No — it only checks marker/existence, never that registries are empty; real generated modules legitimately populate them. |
| Any low-code behavior change? | No — generator feature code, registration write path, and i18n runtime are untouched; build + type-check green. |
| Verification adequate? | Yes — gate, checker tests, existing cleanup gate, Go build/gofmt, frontend type-check, structure/frontmatter/link gates all green. |

## Residual risks

- Newly generated module files (as opposed to the 11 stable artifacts) are not yet
  marker-enforced; the generator needs to emit the marker. Recorded as a follow-up ratchet.
- The marker string is duplicated between the cleanup script and the checker by necessity;
  if they diverge the gate fails loudly (every artifact would read as marker-missing), which
  is a safe failure mode but should be kept in sync deliberately.

## Machine Readable

```json
{
  "taskId": "2026-09-22-generated-artifact-governance",
  "verdict": "approved",
  "findings": [],
  "residualRisks": [
    "Generator does not yet emit the marker on newly generated module files",
    "check-arch-boundaries.mjs checkGeneratedRegistry still overlaps and is not CI-wired"
  ],
  "linkage": {
    "taskManifest": ".harness/tasks/2026-09-22-generated-artifact-governance/manifest.json",
    "evidence": ".harness/evidence/2026-09-22-generated-artifact-governance/commands.json",
    "reviewFile": ".harness/evidence/2026-09-22-generated-artifact-governance/review.md",
    "changeRef": "none",
    "planRefs": [".harness/NAMING_AND_BOUNDARIES_REMEDIATION_PLAN_2026-09-22.md"]
  }
}
```
